{
  "name": "xterm-terminal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint --no-eslintrc --env browser --env es2021 consoleserver/static/terminal.js",
    "check": "prettier --check consoleserver/static/terminal.js",
    "format": "prettier --write consoleserver/static/terminal.js"
  },
  "devDependencies": {
    "eslint": "^8",
    "prettier": "^3"
  }
}
